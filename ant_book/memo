# 競技プログラミングメモ

## スタック
一番最後に入れたやつが一番最初に出てくる
s.pushで要素を入れることができる
s.pop()で一番最後に入れた値が削除される。値は返さない
s.top()で一番最後に入れた値が返却される

## キュー
一番最後に入れたやつが一番最後に出てくる
s.pushで要素を挿入することができる
s.front()で一番最初に入れた値は返却される
s.pop()で一番最初に入れた値が削除される。値は返さない
